machine:
  services:
    - docker
  environment:
    ci: "true"
dependencies:
  cache_directories: []
  override:
    - docker info
test:
  override:
    - make
deployment:
  production:
    branch: master
    owner: opsee
    commands:
      - make push
